# --- User Custom Rules ---
# This space is reserved for personal window rules.
# Add your overrides here to keep them separate from the core configuration.
{
  # Example:
  # wayland.windowManager.hyprland.settings.windowrulev2 = [
  #   "float, class:^(my-special-app)$"
  # ];
}